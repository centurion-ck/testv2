version: "3.8"

services:

  backend:

    build:
      context: ./backend

    container_name: kubeguardian-backend

    ports:
      - "8000:8000"

  frontend:

    build:
      context: ./frontend

    container_name: kubeguardian-frontend

    ports:
      - "80:80"

    depends_on:
      - backend