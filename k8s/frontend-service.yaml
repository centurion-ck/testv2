apiVersion: v1
kind: Service

metadata:
  name: kubeguardian-frontend
  namespace: kubeguardian

spec:
  selector:
    app: kubeguardian-frontend

  ports:
  - port: 80
    targetPort: 80

  type: LoadBalancer